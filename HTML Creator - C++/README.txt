Program creates a hard coded basic html page.

Lets the user input the font type, font color, and graph color. 

Program uses classes to organize html and css and tables.

Program takes data from a csv and parses through it to output to HTML.
HTML files found after project run. 
Output of files to show processing of pages as they are made. 